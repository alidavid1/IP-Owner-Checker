**ReadME
If you have IP addresses as a text you can run the python script or you may download executable file which provides simple GUI. The application takes IP addresses as a text and provides output as an excel file
